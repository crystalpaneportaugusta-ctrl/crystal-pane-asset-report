const Calendar={rrule(f){if(f==="Weekly")return"RRULE:FREQ=WEEKLY";if(f==="Fortnightly")return"RRULE:FREQ=WEEKLY;INTERVAL=2";if(f==="Monthly")return"RRULE:FREQ=MONTHLY";if(f==="Quarterly")return"RRULE:FREQ=MONTHLY;INTERVAL=3";if(f==="Half Yearly")return"RRULE:FREQ=MONTHLY;INTERVAL=6";return""},googleCalendarUrl(c,s){const title="Crystal Pane - "+c.businessName;const start=(s.startDate||"").replaceAll("-","")+"T"+(s.time||"09:00").replace(":","")+"00";const endDate=new Date(`${s.startDate}T${s.time||"09:00"}`);endDate.setMinutes(endDate.getMinutes()+Number(s.duration||60));const end=endDate.toISOString().replace(/[-:]/g,"").split(".")[0]+"Z";let u="https://calendar.google.com/calendar/render?action=TEMPLATE";u+="&text="+encodeURIComponent(title);u+="&location="+encodeURIComponent(c.siteAddress||"");u+="&details="+encodeURIComponent("Routine Crystal Pane service for "+c.businessName);u+="&dates="+encodeURIComponent(start+"/"+end);const recur=this.rrule(s.frequency);if(recur)u+="&recur="+encodeURIComponent(recur);return u}};
